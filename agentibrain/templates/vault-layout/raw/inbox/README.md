# Raw Inbox

`/ingest` drops land here pre-classification. The tick-engine reads this folder, classifies content, and routes it to the right hemisphere + folder.

Do not edit files here directly — they are routed out every tick.
