# Ideas — Creative MUBS Instances

Each idea is its own directory with a MUBS. Ideas may graduate into `left/projects/` once they are being built.

```bash
mkdir right/ideas/<your-idea>
cp -r templates/mubs/* right/ideas/<your-idea>/
```

Use `templates/idea.md` for quick-capture notes that don't yet need full MUBS structure.
