# Unconscious — Cooled But Linked

Arcs that were once hot (≥ 7) but have cooled (heat dropped below 7). Still linked via edges so retrieval finds them, but no longer auto-injected into agent context.

Managed by the brain-keeper daemon. Graduates to `left/` or `right/` when heat drops below 2.
