apiVersion: v2
name: mcp
description: MCP retrieval gateway (kb_search / kb_brief / brain_search_arcs / brain_get_arc) — agentibrain-kernel image
version: 0.1.1
dependencies:
  - name: tcc-k8s-service-template
    version: "0.3.8"
    repository: "oci://ghcr.io/the-cloud-clockwork"
    alias: tpl
