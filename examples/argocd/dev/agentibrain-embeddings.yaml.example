# Sample ArgoCD Application — agentibrain-embeddings (dev).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentibrain-embeddings
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Chart source — kernel
    - repoURL: https://github.com/The-Cloud-Clockwork/agentibrain-kernel.git
      targetRevision: dev
      path: helm/embeddings
      helm:
        releaseName: agentibrain-embeddings
        valueFiles:
          - $values/values-overlays/embeddings/values-dev.yaml
    # $values source — your platform repo
    - repoURL: <your-platform-repo>
      targetRevision: <your-revision>
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: <your-dev-namespace>
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
