# Sample ArgoCD Application — agentibrain-brain-api (dev).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentibrain-brain-api
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Chart source — kernel
    - repoURL: https://github.com/The-Cloud-Clockwork/agentibrain-kernel.git
      targetRevision: dev
      path: helm/brain-api
      helm:
        releaseName: agentibrain-brain-api
        valueFiles:
          - $values/values-overlays/brain-api/values-dev.yaml
    # $values source — your platform repo
    - repoURL: <your-platform-repo>
      targetRevision: <your-revision>
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: <your-dev-namespace>
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
