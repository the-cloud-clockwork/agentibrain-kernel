# Sample ArgoCD Application — agentibrain-obsidian-reader (prod).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentibrain-obsidian-reader-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Chart source — kernel
    - repoURL: https://github.com/The-Cloud-Clock-Work/agentibrain-kernel.git
      targetRevision: main
      path: helm/obsidian-reader
      helm:
        releaseName: agentibrain-obsidian-reader-prod
        valueFiles:
          - $values/values-overlays/obsidian-reader/values-prod.yaml
    # $values source — your platform repo
    - repoURL: <your-platform-repo>
      targetRevision: <your-revision>
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: <your-namespace>
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
