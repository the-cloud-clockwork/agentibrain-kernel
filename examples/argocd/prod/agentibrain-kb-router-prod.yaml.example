# Sample ArgoCD Application — agentibrain-brain-api (prod).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentibrain-brain-api-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Chart source — kernel
    - repoURL: https://github.com/The-Cloud-Clockwork/agentibrain-kernel.git
      targetRevision: main
      path: helm/brain-api
      helm:
        releaseName: agentibrain-brain-api-prod
        valueFiles:
          - $values/values-overlays/brain-api/values-prod.yaml
    # $values source — your platform repo
    - repoURL: <your-platform-repo>
      targetRevision: <your-revision>
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: <your-namespace>
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
