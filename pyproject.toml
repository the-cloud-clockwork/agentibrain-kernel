[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentibrain"
version = "0.15.0"
description = "Standalone brain + knowledge-base kernel (agenti ecosystem)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "The Cloud Clockwork" }]
keywords = ["brain", "knowledge-base", "agenti", "memory", "rag"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "click>=8.1",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "jinja2>=3.1",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.8",
]

[project.scripts]
agentibrain = "agentibrain.cli:main"

[project.urls]
Homepage = "https://github.com/The-Cloud-Clockwork/agentibrain-kernel"
Repository = "https://github.com/The-Cloud-Clockwork/agentibrain-kernel"
Issues = "https://github.com/The-Cloud-Clockwork/agentibrain-kernel/issues"

[tool.setuptools.packages.find]
include = ["agentibrain*"]

[tool.setuptools.package-data]
# The `.claude/` overlay under profiles/ is NOT covered here: setuptools expands
# these with stdlib glob, whose wildcards skip hidden names. MANIFEST.in carries
# the whole profiles tree instead, and is the only thing that survives a build
# from an unpacked sdist (no git, no revision-control file list).
agentibrain = [
    "templates/compose/*.j2",
    "templates/helm/**/*",
    "templates/vault-layout/**/*",
    "migrations/*.sql",
    "profiles/**/*",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
